# .env.example
# Gecko AI Framework Configuration Template
# Copy this file to .env and adjust values for your environment.

# ========================================================
# 🤖 Model & Inference (模型与推理)
# ========================================================
# The primary LLM model to use (e.g., gpt-4o, claude-3-5-sonnet, ollama/llama3)
GECKO_DEFAULT_MODEL=glm-4-flash

# API Key for the model provider
GECKO_DEFAULT_API_KEY=3bd5e6fdc377489c80dbb435b84d7560.izN8bDXCVR1FNSYS

# Optional: Base URL for API proxies or local models (e.g., http://localhost:11434/v1)
# GECKO_DEFAULT_BASE_URL=

# Sampling temperature (0.0 to 2.0). Lower is more deterministic.
GECKO_DEFAULT_TEMPERATURE=0.7

# Request timeout in seconds (Default: 30.0)
GECKO_DEFAULT_MODEL_TIMEOUT=30.0


# ========================================================
# 🕵️ Agent Execution (智能体运行时)
# ========================================================
# Maximum number of conversation turns per run to prevent infinite loops
GECKO_MAX_TURNS=10

# Maximum token limit for context window management
GECKO_MAX_CONTEXT_TOKENS=4000


# ========================================================
# 🌊 Workflow Engine (工作流引擎)
# ========================================================
# Checkpoint strategy: 'final' (recommended for prod), 'always' (debug), 'manual'
GECKO_WORKFLOW_CHECKPOINT_STRATEGY=final

# Number of recent history steps to retain in checkpoints (Context Slimming)
GECKO_WORKFLOW_HISTORY_RETENTION=20


# ========================================================
# 💾 Storage & Database (存储层)
# ========================================================
# Connection URL. Examples:
# - SQLite: sqlite:///./gecko_data.db
# - Redis:  redis://localhost:6379/0
# - Chroma: chroma:///./chroma_db
GECKO_DEFAULT_STORAGE_URL=sqlite:///./gecko_data.db

# Database connection pool size (For SQL/Redis)
GECKO_STORAGE_POOL_SIZE=5

# Maximum number of connections to allow in overflow
GECKO_STORAGE_MAX_OVERFLOW=10


# ========================================================
# 🧠 Memory Management (记忆模块)
# ========================================================
# Debounce interval (seconds) for summary updates
GECKO_MEMORY_SUMMARY_INTERVAL=30.0

# LRU Cache size for token counting
GECKO_MEMORY_CACHE_SIZE=2000

# Tokens reserved for summary injection
GECKO_MEMORY_SUMMARY_RESERVE_TOKENS=500


# ========================================================
# 📡 Telemetry (遥测与监控)
# ========================================================
# Enable OpenTelemetry tracing (requires pip install opentelemetry-sdk)
GECKO_TELEMETRY_ENABLED=True

# Service name for traces
GECKO_TELEMETRY_SERVICE_NAME=gecko-app

# (Optional) OTLP Endpoint for exporting traces (Standard OTel Env Var)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318


# ========================================================
# ⚙️ System (系统设置)
# ========================================================
# Log Level: DEBUG, INFO, WARNING, ERROR
GECKO_LOG_LEVEL=INFO

# Log Format: 'text' (console friendly) or 'json' (machine friendly)
GECKO_LOG_FORMAT=text

# Enable internal caching mechanisms
GECKO_ENABLE_CACHE=True

# Execution timeout for single tool calls (seconds)
GECKO_TOOL_EXECUTION_TIMEOUT=30.0